process {
    // Direct-RNA reads aligned to a transcriptome reference: unspliced, so map-ont.
    // nanopolish's own polya quickstart aligns with map-ont against a transcript reference.
    withName: 'MINIMAP2_ALIGN' {
        ext.args = '-ax map-ont'
    }
}
